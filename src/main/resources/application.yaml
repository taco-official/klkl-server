spring:
  config:
    import: optional:file:.env[.properties]
  profiles:
    group:
      test: "test, h2"
      local: "local, h2"
      dev: "dev, mysql"
    include:
      - swagger
      - storage
      - oauth
      - jwt

  application:
    name: klkl
    uri: ${APP_URL}